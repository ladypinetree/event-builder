<div class="container">
  <h2>Events List</h2>
  <h3> IT WORKS!</h3>

  {{#each model as |event|}}
  <h3>{{#link-to 'event' event}}{{event.eventTitle}}{{/link-to}}</h3>
  {{/each}}
  {{#link-to 'index'}}Back{{/link-to}}
</div>

{{outlet}}
