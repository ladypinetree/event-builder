<div class="container">
  <h3>{{#link-to 'event' event}}{{event.eventTitle}}{{/link-to}}</h3>
  <p>{{#link-to 'edit' event class="btn btn-info btn-s glyphicon-pencil"}} Edit{{/link-to}}
  <button class="btn btn-danger btn-s" {{action 'deleteEvent'}}>Delete</button>
</p>
</div>

{{outlet}}
